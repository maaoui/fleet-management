import { EmployeeWithCredentials } from './employee-with-credentials';

describe('EmployeeWithCredentials', () => {
  it('should create an instance', () => {
    expect(new EmployeeWithCredentials()).toBeTruthy();
  });
});
