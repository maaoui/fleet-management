import { ValidatorConstants } from './validator-constants';

describe('ValidatorConstants', () => {
  it('should create an instance', () => {
    expect(new ValidatorConstants()).toBeTruthy();
  });
});
