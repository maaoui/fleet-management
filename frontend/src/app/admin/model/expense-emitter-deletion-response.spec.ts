import { ExpenseEmitterDeletionResponse } from './expense-emitter-deletion-response';

describe('ExpenseEmitterDeletionResponse', () => {
  it('should create an instance', () => {
    expect(new ExpenseEmitterDeletionResponse()).toBeTruthy();
  });
});
